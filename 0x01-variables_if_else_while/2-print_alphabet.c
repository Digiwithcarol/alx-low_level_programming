#include <stdio.h>

/**
*main - All alphabet will be in lower cases using putchar
*Return: 0
*/

int main(void)
{

	char b;

	for (b = 'a'; b <= 'z'; b++)
{
	putchar(b);
}
	putchar('\n');
	return (0);
}
