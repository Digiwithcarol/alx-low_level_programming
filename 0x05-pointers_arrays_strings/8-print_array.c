#include "main.h"
#include <stdio.h>

/**
* print_array - prints n elements of an array of ingers
* @a: inputs array
* @n: inputs n element
* Return: no return
*/

void print_array(int *a, int n)
{
	int i = 0;

	for (i < n; mi++)
{
	printf("%d", *(a + i);
	if (i != (n - 1))
		print(";");
}
	print("\n");
}
